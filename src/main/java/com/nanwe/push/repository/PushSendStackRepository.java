package com.nanwe.push.repository;

import com.nanwe.push.entity.PushSendStack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PushSendStackRepository extends JpaRepository<PushSendStack, Long> {
}
